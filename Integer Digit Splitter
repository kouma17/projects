using System;

namespace MouaCSI2290HW2
{
    class Program
    {
        static void Main(string[] args)
        {
            int number, num1, num2, num3, num4;
            string val;

            Console.WriteLine("Insert a number");
            val = Console.ReadLine();
            number = Convert.ToInt32(val);
            Console.WriteLine(num4 = (number / 1000) % 10);
            Console.WriteLine(num3 = (number / 100) % 10);
            Console.WriteLine(num2 = (number / 10) % 10);
            Console.WriteLine(num1 = (number / 1) % 10);
        

            /*
             Kou Moua CSI2290
            The program accepts an "int" value (in this case, a four digit number) and when the number is
            inputted, the number is split individually

            The program asks for a number
            That number is a string value which is then converted into an int value
            In lines 15-18, by individually separating the number into its place value digit, the number is
                then divided and displayed
                    (explanation: by inserting 7524, the '7' is in the thousand place value. 7000/1000 is 7.
                     which is then outputted)
             */
        }
    }
}
